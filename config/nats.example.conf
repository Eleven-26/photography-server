# NATS 服务配置
# 配合 docker-compose 启动命令使用：nats-server -c /etc/nats/nats.conf -js -sd /data

# 监听端口（容器内默认 4222）
port: 4222

# HTTP 监控端口
http_port: 8222

# 日志
debug: false
trace: false

# JetStream 配置（-js 命令行参数同样会开启，此处显式声明）
jetstream {
    max_memory_store: 512MB
    max_file_store: 2GB
}
