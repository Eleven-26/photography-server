# 系统架构图（四仓）

> 图用 Mermaid 表达，GitHub/GitLab/VS Code 可直接渲染；纯文本阅读也能看懂。

## 一、总体架构

```mermaid
flowchart TB
  subgraph CLIENT["客户端（三个独立前端仓）"]
    PC["photography-frontend<br/>Vue3 + TS 管理后台 :8081"]
    WX["photography-wechat<br/>uni-app 员工端 :8083"]
    H5["photography-h5<br/>uni-app 客户端 :8082"]
  end
  subgraph EDGE["接入层"]
    NG["Nginx / Vite proxy<br/>剥离 /api 前缀"]
  end
  subgraph BE["photography-server（Go 1.26 · Gin）"]
    RT["router 路由装配<br/>端点声明 + 中间件 + 静态资源"]
    MW["middleware 链<br/>CORS/Recovery/Log/Params/Auth/Perm"]
    CT["presentation<br/>controller / h5 / wechat"]
    SV["service 业务编排（39 文件）"]
    RP["repository 数据访问（24 文件）<br/>WithTx + company_id 租户过滤"]
    DM["domain / enum / model<br/>状态机 · 退款规则 · 领域纯函数"]
  end
  subgraph INFRA["基础设施（docker compose）"]
    DB[("MySQL 8")]
    RD[("Redis")]
    NT[("NATS JetStream")]
    ES[("Elasticsearch")]
    MG[("MongoDB")]
    XX["XXL-JOB"]
    NC["Nacos 配置 + 注册"]
    TR["Jaeger / SkyWalking"]
  end
  PC --> NG
  WX --> NG
  H5 --> NG
  NG --> RT --> MW --> CT --> SV --> RP --> DB
  SV --> DM
  SV --> RD
  SV --> NT
  RP --> ES
  RP --> MG
  XX --> BE
  BE -. 配置拉取 / 服务注册 .-> NC
  BE -. 链路上报 .-> TR
```

## 二、端与挂载点

| 端 | 前缀 | 控制器 | 鉴权 |
| --- | --- | --- | --- |
| PC 管理后台 | 无 | `presentation/controller` | `Auth`（员工 JWT）+ `Perm`（RBAC 权限点） |
| 小程序管理后台 | `/miniapp` | 同 PC，同一 Handler | 同 PC |
| 客户 H5 | `/h5` | `presentation/h5` | 公开组免鉴权 + `CustomerAuth` 组 |
| 微信客户区 | `/wechat` | 同 H5，同一 Handler | 同 H5 |
| 员工小程序 | `/wechat/staff` | `presentation/wechat`（独有）+ 复用 PC handler | `StaffAuth` + `Perm` |

路由是**声明式**的：`presentation/routes`（有哪些路由）→ `presentation/endpoint`（怎么挂）→ `router/endpoints.go`（每个端暴露哪些）；同 Path 跨端必须是同一函数指针（护栏测试）。

## 三、后端请求链路

```mermaid
sequenceDiagram
  autonumber
  participant C as 客户端
  participant G as Gin Engine
  participant P as params.Middleware
  participant A as Auth / Perm
  participant H as Handler（controller/h5/wechat）
  participant S as Service
  participant R as Repository
  participant D as MySQL
  C->>G: POST /{module}/{action}（JSON body，可带 :id）
  G->>P: 预解析 body 存入 context（供 params.* 读取）
  P->>A: 认证（员工/客户 JWT）+ RBAC 权限点 + 数据范围
  A->>H: 注入 Operator（user_id/company_id/store_id/data_scope）
  H->>S: 调用业务方法（ctx, op, req）
  S->>R: 事务/查询（WithTx 透传）
  R->>D: GORM + company_id 租户过滤
  D-->>R: 行数据
  R-->>S: model
  S-->>H: contract DTO
  H-->>C: response.Body{code,msg,data,trace_id}
```

关键约定：

- 业务接口一律 `POST` + JSON body；`create/update` 主键在 body，`detail/status/delete` 主键在路径。
- 统一响应 `Body{code,msg,data,trace_id}`；列表 `Page{list,total,page,page_size}`。
- 错误经 `errs.BizError` 归一；5xx 抓构造点堆栈并写 Jaeger。
- 依赖单向：`presentation -> service -> repository -> model/domain`；业务层不 `import infrastructure`，只依赖 `app.App`。

## 四、前端各仓内部分层

```mermaid
flowchart LR
  subgraph PC["photography-frontend"]
    V["views（15）"] --> CMP["components（2）"]
    V --> CMP2["composables（useFetch/useToast）"]
    V --> ST["stores（auth/app）"]
    V --> API1["api/*（17）"]
    API1 --> HC["api/common/http.ts<br/>axios + 拦截器 + token"]
    HC --> AP1["api/common/apiPath.ts<br/>API_PATHS 单一来源"]
    R1["router/index.ts<br/>meta.perm + 登录守卫"] --> V
  end
  subgraph UNI["photography-wechat / photography-h5（uni-app）"]
    PG["pages/*（51 / 33）"] --> C2["components（App*，easycom）"]
    PG --> ST2["stores（user.js）"]
    PG --> API2["api/*（20 / 16）"]
    API2 --> HC2["api/common/http.js<br/>uni.request + rpc + 401"]
    HC2 --> AP2["api/common/apiPath.js"]
    PJ["pages.json"] -. 声明路由 .-> PG
  end
```

## 五、部署拓扑（docker-compose，backend 仓统一编排）

```mermaid
flowchart LR
  U["浏览器 / 微信"] --> FE["frontend :8081"]
  U --> H5C["h5 :8082"]
  U --> WXC["wechat :8083"]
  FE -->|"/api 反代"| BE["backend :8080"]
  H5C -->|"/api 反代"| BE
  WXC -->|"/api 反代"| BE
  U -->|"/media 直读（不剥前缀）"| M["公开媒体目录"]
  BE --> MYSQL[("mysql")]
  BE --> REDIS[("redis")]
  BE --> NATS[("nats")]
  BE --> NACOS[("nacos")]
  NACOS --> NACOSINIT["nacos-init（一次性：建管理员 + 发布配置）"]
  BE --> JAEGER[("jaeger")]
  JAEGER --> CH[("clickhouse")]
  BE -. 可选 debug profile .-> ES[("elasticsearch")]
  BE -. 可选 debug profile .-> MONGO[("mongodb")]
  BE --> XXL["xxl-job-admin"]
```

> 端口与依赖关系以 `photography-server/docker-compose.yml` 为准；`debug` profile 才启动 ES / MongoDB。

## 六、鉴权与多租户

```mermaid
flowchart TB
  REQ["请求"] --> JWT{"Authorization: Bearer ?"}
  JWT -->|员工令牌| OP["Operator: user_id / role_id / company_id / store_id"]
  JWT -->|客户令牌| COP["客户上下文: customer_id / company_id"]
  OP --> PERM{"RBAC：domain.Perm 权限点"}
  PERM -->|"有权限点"| SCOPE{"数据范围 data_scope<br/>1 全部 / 2 本店 / 3 本人"}
  PERM -->|"无/为空"| OPEN["放行（自助类/通用能力）"]
  SCOPE --> Q["repository 查询附带 company_id（+ store_id/user_id）过滤"]
  COP --> Q
```

- 多租户：所有业务表含 `company_id`，`repository` 统一带租户过滤；客户区用 slug 反查 `company_id`，不接受客户端直传。
- 权限点：`domain/perm.go` 定义，`sys_role_permission` 存储；端上按 `Perm` 中间件挂载。