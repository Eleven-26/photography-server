# 占位文件：保证 build/agent/ 目录在仓库中存在（Dockerfile 的 COPY build/agent/ 依赖该目录）。
# 真正的 agent 二进制（skywalking-go-agent-<version>-linux-amd64，约 45MB）已被 .gitignore 忽略，
# 需自行从官方 bin.tgz 解压到本目录后再做注入构建（SW_AGENT_ENABLE=true）。
