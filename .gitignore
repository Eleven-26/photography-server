# binaries
/photography-server
/server
*.exe
bin

# skywalking-go agent 二进制：有意随仓库入库（约 45MB，勿加忽略规则）
# 原因：Dockerfile 是离线构建（不联网下载），新环境 clone 后必须能直接 COPY 进镜像；
# 若要改为不入库，必须同步给 Dockerfile 加「本地缺失时自动下载」的 fallback，否则注入构建必然失败。

# env
.env

# nacos SDK 本地快照与日志（cache 里是从配置中心拉取的配置明文，含运行时注入前的完整内容，严禁入库）
.nacos/

# config local
config/config.local.yaml

# uploads
uploads/

# logs
logs/
*.log

# IDE
.idea/
.vscode/
*.swp

# os
.DS_Store
Thumbs.db
.workbuddy
